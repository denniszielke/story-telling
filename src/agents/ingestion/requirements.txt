# Ingestion agent — runtime dependencies (baked into the BYOC sandbox image).
azure-identity>=1.17.0
azure-search-documents>=11.5.0
openai>=1.40.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0
PyYAML>=6.0.0
# Required because src/content/__init__.py eagerly imports the PDF extractor (fitz).
pymupdf>=1.27.2.3
